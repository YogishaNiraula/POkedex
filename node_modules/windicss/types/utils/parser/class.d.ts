import type { Element } from '../../interfaces';
export default class ClassParser {
    index: number;
    separator: string;
    classNames?: string;
    constructor(classNames?: string, separator?: string);
    private _handle_group;
    private _handle_function;
    parse(removeDuplicated?: boolean): Element[];
}
